<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplicativo_localizacao_indoor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".adm.RemoverActivity" ></activity>
        <activity android:name=".adm.AlterarActivity" />
        <activity android:name=".adm.InserirActivity" />
        <activity android:name=".adm.MenuAdmActivity" />
        <activity android:name=".activity.LocalizarActivity" android:label="@string/btLocalizar"/>
        <activity android:name=".activity.BuscarRotaActivity" android:label="@string/btBuscaRota"/>
        <activity android:name=".activity.LoginActivity" android:label="@string/mn_login"/>
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>